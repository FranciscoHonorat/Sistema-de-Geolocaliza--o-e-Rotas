async exercutar(input: DTO): Promise<OutputDTO> {
    // 1. Validar entrada (estrutura básica)
    
    // 2. Buscar no cache (performance)
    // 3. Converter DTO → Domain Objects
    // 4. Executar lógica de domínio
    // 5. Converter resultado → DTO saída
    // 6. Salvar no cache
    // 7. Retornar resultado
}