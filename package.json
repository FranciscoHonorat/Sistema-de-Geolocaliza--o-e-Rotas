{
  "name": "sistema-geolocalizacao-rotas",
  "version": "1.0.0",
  "description": "Sistema de geolocalização e cálculo de rotas",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node teste-exemplo.ts",
    "dev:server": "nodemon --exec ts-node server.ts",
    "server": "ts-node server.ts",
    "server:watch": "nodemon --exec ts-node server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "start:prod": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"Linting não configurado ainda\"",
    "clean": "rm -rf dist || rmdir /s dist"
  },
  "keywords": [
    "geolocalização",
    "rotas",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.5.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.1",
    "nodemon": "^3.1.10",
    "postgis": "^1.0.6"
  }
}
